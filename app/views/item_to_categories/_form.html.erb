<%= form_for(item_to_category) do |f| %>
  <% if item_to_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(item_to_category.errors.count, "error") %> prohibited this item_to_category from being saved:</h2>

      <ul>
      <% item_to_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :item_id %>
    <%= f.number_field :item_id %>
  </div>

  <div class="field">
    <%= f.label :category_id %>
    <%= f.number_field :category_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
